<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">
    <item android:id="@+id/action_logout" android:title="Uitloggen"
        app:showAsAction="never" />

    <item android:id="@+id/action_post_tweet" android:icon="@android:drawable/ic_menu_edit" android:title="post"
        app:showAsAction="always|withText" />

    <item android:id="@+id/action_search_tweet" android:icon="@android:drawable/ic_search_category_default" android:title="search"
        app:showAsAction="always|withText" />

</menu>
